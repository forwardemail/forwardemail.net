= striptags(`${t('Your plan <span class="notranslate">%s</span> <span class="notranslate">%s</span>', new Date(user[config.userFields.planExpiresAt]).getTime() > Date.now() ? t('expires') : t('expired'), dayjs(user[config.userFields.planExpiresAt]).locale(locale).fromNow())} – ${_.isDate(user[config.userFields.paymentReminderFollowUpSentAt]) ? t('final notice!') : _.isDate(user[config.userFields.paymentReminderInitialSentAt]) ? t('follow-up reminder!') : t('make payment to keep your account active')}`)
