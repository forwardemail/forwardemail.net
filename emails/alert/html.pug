extends ../layout

block content
  .container.mt-3
    .row
      .col-12
        .card.d-block
          h5.card-header.text-center= t("System Alert")
          .card-body
            p.card-text!= message
          div: small.card-footer.text-center.text-muted= t("This is an automated system alert.")
