= striptags(t('<span class="notranslate">Forward Email</span> Receipt [<span class="notranslate">%s</span>] (<span class="notranslate">%s</span>)', payment.reference.toUpperCase(), dayjs(payment.invoice_at).format('M/D/YY')))
