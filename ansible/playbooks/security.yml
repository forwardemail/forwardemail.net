# Copyright (c) Forward Email LLC
# SPDX-License-Identifier: BUSL-1.1

---
- name: Import Unbound DNS caching playbook
  import_playbook: unbound.yml
  vars:
    target_hosts: "{{ security_target_hosts | default('all') }}"

- name: Import System Optimization playbook
  import_playbook: system-optimization.yml
  vars:
    target_hosts: "{{ security_target_hosts | default('all') }}"

- name: Import AMD Ryzen NUMA Optimization playbook
  import_playbook: amd-ryzen-numa.yml
  vars:
    target_hosts: "{{ security_target_hosts | default('all') }}"
    service_name: "System"
    disable_thp: false

- name: Import Chrony time synchronization playbook
  import_playbook: chrony-timesync.yml
  vars:
    target_hosts: "{{ security_target_hosts | default('all') }}"

- hosts: "{{ security_target_hosts | default('all') }}"
  name: Security
  become: true
  become_user: root
  vars:
    copy_local_key: "{{ lookup('file', lookup('env','HOME') + '/.ssh/id_rsa.pub') }}"
    copy_local_key_2: "{{ lookup('file', lookup('env','HOME') + '/.ssh/id_rsa_2.pub') }}"
    alert_email_sender: "{{ lookup('env', 'ALERT_EMAIL_FROM') | default('mailerdaemon@forwardemail.net', true) }}"
    alert_email_recipients: "{{ lookup('env', 'ALERT_EMAIL_RECIPIENTS') | default(lookup('env', 'MSMTP_RCPTS') | default('security@forwardemail.net', true), true) }}"
    twilio_account_sid: "{{ lookup('env', 'TWILIO_ACCOUNT_SID') }}"
    twilio_auth_token: "{{ lookup('env', 'TWILIO_AUTH_TOKEN') }}"
    twilio_from_number: "{{ lookup('env', 'TWILIO_FROM_NUMBER') }}"
    twilio_to_number: "{{ lookup('env', 'TWILIO_TO_NUMBER') }}"
    github_octokit_token: "{{ lookup('env', 'GITHUB_OCTOKIT_TOKEN') }}"
    systemd_incident_failure_threshold: >-
      {{ lookup('env', 'SYSTEMD_INCIDENT_FAILURE_THRESHOLD') | default('3', true) | int }}
    systemd_incident_recovery_threshold: >-
      {{ lookup('env', 'SYSTEMD_INCIDENT_RECOVERY_THRESHOLD') | default('3', true) | int }}
    systemd_incident_observation_interval_seconds: 60
    systemd_incident_api_url: "https://api.github.com"
    outbound_traffic_monitor_enabled: >-
      {{ lookup('env', 'OUTBOUND_TRAFFIC_MONITOR_ENABLED') | default('true', true) | bool }}
    outbound_traffic_monitor_interface: >-
      {{ lookup('env', 'OUTBOUND_TRAFFIC_MONITOR_INTERFACE') | default('auto', true) }}
    outbound_traffic_monitor_threshold_bps: >-
      {{ lookup('env', 'OUTBOUND_TRAFFIC_MONITOR_THRESHOLD_BPS') | default('1000000000', true) | int }}
    outbound_traffic_monitor_interval_seconds: >-
      {{ lookup('env', 'OUTBOUND_TRAFFIC_MONITOR_INTERVAL_SECONDS') | default('15', true) | int }}
    outbound_traffic_monitor_consecutive_breaches: >-
      {{ lookup('env', 'OUTBOUND_TRAFFIC_MONITOR_CONSECUTIVE_BREACHES') | default('2', true) | int }}
    outbound_traffic_monitor_top_lines: >-
      {{ lookup('env', 'OUTBOUND_TRAFFIC_MONITOR_TOP_LINES') | default('25', true) | int }}
    systemd_incident_host_map:
      mongo.forwardemail.net:
        component: mongo.forwardemail.net
        label: mongo-forwardemail-net
        units:
          - mongod.service
      logs.forwardemail.net:
        component: logs.forwardemail.net
        label: logs-forwardemail-net
        units:
          - mongod.service
      redis.forwardemail.net:
        component: redis.forwardemail.net
        label: redis-forwardemail-net
        units:
          - valkey-server.service
      bree.forwardemail.net:
        component: bree.forwardemail.net
        label: bree-forwardemail-net
        units:
          - pm2-deploy.service
          - pm2-health-check.service
      forwardemail.net:
        component: forwardemail.net
        label: forwardemail-net-443-i-pv4
        units:
          - pm2-deploy.service
          - pm2-health-check.service
      api.forwardemail.net:
        component: api.forwardemail.net
        label: api-forwardemail-net-443-i-pv4
        units:
          - pm2-deploy.service
          - pm2-health-check.service
      caldav.forwardemail.net:
        component: caldav.forwardemail.net
        label: caldav-forwardemail-net-443-i-pv4
        units:
          - pm2-deploy.service
          - pm2-health-check.service
      carddav.forwardemail.net:
        component: carddav.forwardemail.net
        label: carddav-forwardemail-net-443-i-pv4
        units:
          - pm2-deploy.service
          - pm2-health-check.service
      imap.forwardemail.net:
        component: imap.forwardemail.net
        label: imap-forwardemail-net-993-i-pv4
        units:
          - pm2-deploy.service
          - pm2-health-check.service
      mx1.forwardemail.net:
        component: mx1.forwardemail.net
        label: mx1-forwardemail-net-25-i-pv4
        units:
          - pm2-deploy.service
          - pm2-health-check.service
      mx2.forwardemail.net:
        component: mx2.forwardemail.net
        label: mx2-forwardemail-net-25-i-pv4
        units:
          - pm2-deploy.service
          - pm2-health-check.service
      pop3.forwardemail.net:
        component: pop3.forwardemail.net
        label: pop3-forwardemail-net-995-i-pv4
        units:
          - pm2-deploy.service
          - pm2-health-check.service
      smtp.forwardemail.net:
        component: smtp.forwardemail.net
        label: smtp-forwardemail-net-587-i-pv4
        units:
          - pm2-deploy.service
          - pm2-health-check.service
      sqlite.forwardemail.net:
        component: sqlite.forwardemail.net
        label: sqlite-forwardemail-net
        units:
          - pm2-deploy.service
          - pm2-health-check.service
  pre_tasks:
    # Each entrypoint sets this server's hostname before importing this play.
    # Read the live hostname directly because provider PTR data can differ from
    # the hostname Postfix advertises.
    - name: Read this server's alert hostname
      ansible.builtin.command: hostname
      register: forwardemail_live_hostname
      changed_when: false
      check_mode: false
      tags:
        - always
        - forwardemail-alert-hostname

    - name: Choose this server's alert hostname
      ansible.builtin.set_fact:
        forwardemail_expected_alert_hostname: >-
          {{
            security_expected_alert_hostname
            | default(
                hostvars[inventory_hostname]['alert_hostname']
                | default(
                    hostvars[inventory_hostname]['direct_alert_helo_identity']
                    | default(forwardemail_live_hostname.stdout, true),
                    true
                  ),
                true
              )
            | trim
            | lower
            | regex_replace('\\.$', '')
          }}
      tags:
        - always
        - forwardemail-alert-hostname

    - name: Verify this server's alert hostname
      ansible.builtin.assert:
        that:
          - >-
            (forwardemail_expected_alert_hostname |
            default('', true) | length) > 0
          - >-
            (forwardemail_live_hostname.stdout | default('', true) |
            trim | length) > 0
          - >-
            (forwardemail_live_hostname.stdout | trim | lower |
            regex_replace('\\.$', '')) ==
            forwardemail_expected_alert_hostname
        fail_msg: >-
          Refusing to configure alerts on {{ inventory_hostname }}: expected
          {{ forwardemail_expected_alert_hostname | default('UNSET', true) }},
          but this server is named
          {{ forwardemail_live_hostname.stdout | default('UNAVAILABLE', true) }}.
          Set this server's hostname before importing security.yml.
      tags:
        - always
        - forwardemail-alert-hostname

    - name: Save this server's alert settings
      ansible.builtin.set_fact:
        forwardemail_alert_public_ip: >-
          {{
            hostvars[inventory_hostname]['alert_public_ip']
            | default(
                hostvars[inventory_hostname]['direct_alert_public_ip']
                | default(ansible_host | default(ansible_default_ipv4.address), true),
                true
              )
          }}
        forwardemail_alert_hostname: >-
          {{ forwardemail_expected_alert_hostname }}
      tags:
        - always
        - forwardemail-alert-hostname

    - name: Select this server's public status component
      ansible.builtin.set_fact:
        forwardemail_systemd_incident_config: >-
          {{
            systemd_incident_host_map[
              forwardemail_alert_hostname
            ]
            | default({})
          }}
      tags:
        - always
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Validate SMS services for this server
      ansible.builtin.assert:
        that:
          - >-
            (security_sms_services | default([]) | type_debug) == 'list'
          - >-
            (security_sms_services | default([]) |
            difference([
              'mongod.service',
              'valkey-server.service',
              'pm2-deploy.service',
              'pm2-health-check.service'
            ]) | length) == 0
        fail_msg: >-
          security_sms_services may contain only MongoDB, Valkey, PM2 startup,
          or PM2 health-check services.
      tags:
        - always
        - forwardemail-alert-policy

    - name: Choose SMS services for this server
      ansible.builtin.set_fact:
        forwardemail_sms_services: >-
          {{
            (
              (forwardemail_systemd_incident_config.units | default([]))
              if (forwardemail_systemd_incident_config | length > 0)
              else []
            )
            | union(security_sms_services | default([]))
            | unique
            | list
          }}
      tags:
        - always
        - forwardemail-alert-policy

    - name: Derive public systemd incident reporter state
      ansible.builtin.set_fact:
        forwardemail_systemd_incident_component: >-
          {{ forwardemail_systemd_incident_config.component | default('') }}
        forwardemail_systemd_incident_label: >-
          {{ forwardemail_systemd_incident_config.label | default('') }}
        forwardemail_systemd_incident_units: >-
          {{ forwardemail_systemd_incident_config.units | default([]) }}
        forwardemail_systemd_incident_enabled: >-
          {{
            (forwardemail_systemd_incident_config | length > 0) and
            (github_octokit_token | length > 0)
          }}
      no_log: true
      tags:
        - always
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Derive public systemd incident mapping key
      ansible.builtin.set_fact:
        forwardemail_systemd_incident_mapping_key: >-
          {{
            (
              forwardemail_systemd_incident_component ~ ':' ~
              forwardemail_systemd_incident_label ~ ':' ~
              (forwardemail_systemd_incident_units | join(':'))
            )
            if (
              (forwardemail_systemd_incident_config | length > 0) and
              ((forwardemail_systemd_incident_units | type_debug) == 'list')
            )
            else ''
          }}
      no_log: true
      tags:
        - always
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    # An override must never let one server use another server's hostname.
    - name: Verify saved alert hostname matches this server
      ansible.builtin.assert:
        that:
          - >-
            (forwardemail_live_hostname.stdout | default('', true) |
            trim | length) > 0
          - >-
            (forwardemail_alert_hostname | default('', true) |
            lower | regex_replace('\\.$', '')) ==
            (forwardemail_live_hostname.stdout | default('', true) | trim |
            lower | regex_replace('\\.$', ''))
          - >-
            forwardemail_alert_hostname ==
            forwardemail_expected_alert_hostname
        fail_msg: >-
          Refusing to configure alerts on {{ inventory_hostname }}: the saved
          alert hostname is {{ forwardemail_alert_hostname | default('UNSET', true) }},
          but this server is named
          {{ forwardemail_live_hostname.stdout | default('UNAVAILABLE', true) }}.
          The alert hostname must match this server.
      tags:
        - always
        - forwardemail-alert-hostname

  handlers:
    - name: Restart SSH
      service:
        name: ssh
        state: restarted

    - name: Reload auditd rules
      command: augenrules --load
      changed_when: false
    - name: Restart rsyslog
      service:
        name: rsyslog
        state: restarted

    - name: Restart Postfix
      service:
        name: postfix
        state: restarted

    - name: Restart monitoring services
      systemd:
        daemon_reload: yes
        name: "{{ item }}"
        state: restarted
      loop:
        - system-resource-monitor.timer
        - ssh-security-monitor.timer
        - usb-device-monitor.timer
        - root-access-monitor.timer
        - lynis-audit-monitor.timer
        - package-monitor.timer
        - open-ports-monitor.timer
        - ssl-certificate-monitor.timer
        - outbound-traffic-monitor.timer
  tasks:
    # Ensure netplan is installed and protected from removal
    - name: Ensure netplan is installed
      apt:
        name: netplan.io
        state: present
        update_cache: no

    - name: Hold netplan package to prevent removal
      dpkg_selections:
        name: netplan.io
        selection: hold

    - name: Delete Digital Ocean Networking Configuration
      ansible.builtin.file:
        state: absent
        path: /etc/systemd/resolved.conf.d/DigitalOcean.conf

    - name: Remove Ubuntu Advantage Tools
      apt:
        pkg: ubuntu-advantage-tools
        state: absent

    - name: Purge Ubuntu Advantage Tools
      apt:
        autoremove: yes
        purge: true
        name:
          - ubuntu-advantage-tools

    - name: Insert/Update disable core dumps
      blockinfile:
        path: /etc/security/limits.conf
        state: present
        block: |
          * hard core 0
          * soft core 0

    # https://www.mongodb.com/docs/manual/tutorial/transparent-huge-pages/
    # https://unix.stackexchange.com/questions/99154/disable-transparent-hugepages
    # https://stackoverflow.com/questions/51246128/disabling-thp-transparent-hugepages-with-ansible-role
    - name: "Disable Transparent Huge Pages (THP)"
      template:
        src: "{{ playbook_dir }}/templates/thp.j2"
        dest: /etc/systemd/system/disable-transparent-huge-pages.service
        owner: root
        mode: "0644"

    - name: "Enable Disabled THP Systemd Service"
      service:
        daemon_reload: yes
        name: disable-transparent-huge-pages
        enabled: true
        state: started

    - name: Ensure fs.suid_dumpable is set to 0 and added in sysctl
      ansible.posix.sysctl:
        name: fs.suid_dumpable
        value: "0"
        sysctl_set: true

    - name: Insert/Update disable core dumps
      blockinfile:
        path: /etc/profile
        state: present
        block: |
          ulimit -S -c 0 > /dev/null 2>&1

    # Disable swap entirely on hosts that do not manage emergency swap.
    # MongoDB, Redis, and logs hosts retain swap as a last resort with low
    # swappiness configured by their service playbooks.
    # https://unix.stackexchange.com/a/636526
    - name: Disable swap for current session
      command: swapoff -a
      when: group_names | intersect(['mongo', 'redis', 'logs']) | length == 0
    - name: Disable swap permanently, persist reboots
      replace:
        path: /etc/fstab
        regexp: '^(\s*)([^#\n]+\s+)(\w+\s+)swap(\s+.*)$'
        replace: '#\1\2\3swap\4'
        backup: yes
      when: group_names | intersect(['mongo', 'redis', 'logs']) | length == 0
    - name: Disable swap on boot via systemd
      template:
        src: "{{ playbook_dir }}/templates/noswap.j2"
        dest: /etc/systemd/system/noswap.service
        owner: root
        mode: "0644"
      when: group_names | intersect(['mongo', 'redis', 'logs']) | length == 0
    - name: Enable noswap service via systemd
      systemd:
        daemon_reload: yes
        name: noswap
        enabled: true
        state: started
      when: group_names | intersect(['mongo', 'redis', 'logs']) | length == 0

    - name: Inspect stale noswap service on swap-enabled hosts
      stat:
        path: /etc/systemd/system/noswap.service
        follow: false
      register: stale_noswap_service
      when: group_names | intersect(['mongo', 'redis', 'logs']) | length > 0

    - name: Disable stale noswap service on swap-enabled hosts
      systemd:
        daemon_reload: yes
        name: noswap
        enabled: false
        state: stopped
      when:
        - group_names | intersect(['mongo', 'redis', 'logs']) | length > 0
        - stale_noswap_service.stat.exists | default(false)

    - name: Remove stale noswap service on swap-enabled hosts
      file:
        path: /etc/systemd/system/noswap.service
        state: absent
      when: group_names | intersect(['mongo', 'redis', 'logs']) | length > 0

    # Disable Modprobe Loading of USB Storage Driver
    # https://docs.datadoghq.com/security/default_rules/xccdf-org-ssgproject-content-rule-kernel-module-usb-storage-disabled/
    - name: Ensure kernel module 'usb-storage' is disabled
      lineinfile:
        create: true
        dest: /etc/modprobe.d/usb-storage.conf
        regexp: install\s+usb-storage
        line: install usb-storage /bin/true

    - name: Update initramfs
      shell: update-initramfs -u
      register: initramfs_result
      retries: 3
      delay: 5
      until: initramfs_result.rc == 0
      changed_when: initramfs_result.rc == 0

    # TODO: Disable Accepting ICMP Redirects for All IPv4 Interfaces
    #       https://docs.datadoghq.com/security/default_rules/xccdf-org-ssgproject-content-rule-sysctl-net-ipv4-conf-all-accept-redirects/#ansible-playbook

    # Disable Apport Service
    # https://docs.datadoghq.com/security/default_rules/xccdf-org-ssgproject-content-rule-service-apport-disabled/#ansible-playbook
    - name: Check if apport service exists
      stat:
        path: /lib/systemd/system/apport.service
      register: apport_service

    - name: Disable service apport
      systemd:
        name: apport.service
        enabled: 'no'
        state: stopped
        masked: 'yes'
      when: apport_service.stat.exists
      register: apport_disable
      failed_when: false
    # NOTE: this does not seem necessary since apport.socket does not exist
    #- name: Disable socket apport
    #  systemd:
    #    name: apport.socket
    #    enabled: 'no'
    #    state: stopped
    #    masked: 'yes'
    - name: Disable apport via config
      block:
        - name: Deduplicate values from /etc/default/apport
          lineinfile:
            path: /etc/default/apport
            create: false
            line: enabled=1
            state: absent
        - name: Insert correct line to /etc/default/apport
          lineinfile:
            path: /etc/default/apport
            create: false
            line: enabled=0
            state: present

    #
    # Disable coredump backtraces
    # https://docs.datadoghq.com/security/default_rules/xccdf-org-ssgproject-content-rule-coredump-disable-backtraces/
    #
    - name: Disable core dump backtraces
      block:
        - name: Check for duplicate values
          lineinfile:
            path: /etc/systemd/coredump.conf
            create: true
            regexp: ^\s\*ProcessSizeMax\s\*=\s\*
            state: absent
          check_mode: true
          changed_when: false
          register: dupes
        - name: Deduplicate values from /etc/systemd/coredump.conf
          lineinfile:
            path: /etc/systemd/coredump.conf
            create: true
            regexp: ^\s\*ProcessSizeMax\s\*=\s\*
            state: absent
          when: dupes.found is defined and dupes.found > 1
        - name: Insert correct line to /etc/systemd/coredump.conf
          lineinfile:
            path: /etc/systemd/coredump.conf
            create: true
            regexp: ^\s\*ProcessSizeMax\s\*=\s\*
            line: ProcessSizeMax=0
            state: present

    # update deps
    - name: Update apt
      apt:
        update_cache: true

    # upgrade deps
    - name: Upgrade deps
      apt:
        upgrade: safe
        update_cache: true

    # Preseed Postfix as local-only during package installation.  Ansible
    # installs the final send-only direct-MX configuration before restarting it.
    - name: Preseed Postfix mailer type
      debconf:
        name: postfix
        question: postfix/main_mailer_type
        value: Local only
        vtype: select

    - name: Preseed Postfix system mail name
      debconf:
        name: postfix
        question: postfix/mailname
        value: "{{ ansible_fqdn }}"
        vtype: string

    # install deps
    - name: Install deps
      apt:
        name:
          - iptables
          - ipset
          - build-essential
          - curl
          - ufw
          - git
          - vim
          - wget
          - libtool
          - automake
          - autoconf
          - nasm
          - ripgrep
          - redis-tools
          - postfix
          - spf-tools-perl
          - tpm2-tools
          - libtss2-dev
        update_cache: true

    # Verify TPM 2.0 availability (optional — cloud/VPS hosts may not have TPM hardware)
    - name: Check if TPM device exists
      stat:
        path: /sys/class/tpm/tpm0/tpm_version_major
      register: tpm_device

    - name: Verify TPM 2.0 is available
      command: cat /sys/class/tpm/tpm0/tpm_version_major
      register: tpm_version
      changed_when: false
      failed_when: tpm_version.stdout != '2'
      when: tpm_device.stat.exists

    - name: Warn if TPM is not available
      debug:
        msg: "WARNING: TPM 2.0 is not available on this host. Hardware-backed security features will not be available. This is expected on cloud/VPS hosts."
      when: not tpm_device.stat.exists

    # prevents port scanning
    # https://github.com/forwardemail/portscan-protection
    # (we maintain our own fork since it has an auto-update in place)
    # https://unix.stackexchange.com/a/580520
    # Deploy from local file to avoid GitHub rate limiting
    - name: Copy portscan protection script to server
      copy:
        src: "{{ playbook_dir }}/files/portscan-protection.sh"
        dest: /tmp/portscan-protection.sh
        mode: '0755'

    - name: Run portscan protection script
      shell: bash /tmp/portscan-protection.sh -i
      register: port_scan
      failed_when: port_scan.rc != 1

    - name: Remove temporary portscan script
      file:
        path: /tmp/portscan-protection.sh
        state: absent

    # Infrastructure alerts must not depend on the production SMTP service,
    # application queue, or MongoDB.  Postfix keeps a small local retry queue
    # and delivers directly to each recipient domain's MX over outbound port 25.
    - name: Configure send-only direct-to-MX Postfix
      template:
        src: "{{ playbook_dir }}/templates/postfix-alerts-main.cf.j2"
        dest: /etc/postfix/main.cf
        owner: root
        group: root
        mode: '0644'
      notify: Restart Postfix

    - name: Remove legacy production SMTP relay packages
      apt:
        name:
          - msmtp
          - msmtp-mta
        state: absent
        purge: true

    - name: Remove legacy production SMTP relay credentials and logs
      file:
        path: "{{ item }}"
        state: absent
      loop:
        - /etc/msmtprc
        - /var/log/msmtp.log

    - name: Verify Postfix owns the local sendmail submission binary
      command: dpkg-query -S /usr/sbin/sendmail
      register: postfix_sendmail_owner
      changed_when: false
      failed_when: >-
        postfix_sendmail_owner.rc != 0 or
        'postfix: /usr/sbin/sendmail' not in postfix_sendmail_owner.stdout_lines

    - name: Enable send-only Postfix
      systemd:
        name: postfix
        enabled: true
        state: started

    - name: Apply direct-to-MX Postfix configuration before validation
      meta: flush_handlers

    - name: Validate Postfix configuration
      command: postfix check
      changed_when: false

    - name: Verify hardened send-only Postfix runtime configuration
      shell: |
        set -eu

        assert_postconf() {
          parameter="$1"
          expected="$2"
          actual="$(postconf -h "$parameter")"
          if [ "$actual" != "$expected" ]; then
            printf '%s: expected <%s>, got <%s>\n' \
              "$parameter" "$expected" "$actual" >&2
            exit 1
          fi
        }

        assert_postconf master_service_disable 'inet'
        assert_postconf inet_interfaces 'loopback-only'
        assert_postconf inet_protocols 'ipv4'
        assert_postconf authorized_submit_users 'root'
        assert_postconf relayhost ''
        assert_postconf relay_domains ''
        assert_postconf mynetworks ''
        assert_postconf mydestination ''
        assert_postconf default_transport 'smtp'
        assert_postconf local_transport \
          'error:local mailbox delivery is disabled'
        assert_postconf virtual_transport \
          'error:virtual mailbox delivery is disabled'
        assert_postconf relay_transport \
          'error:relay-domain delivery is disabled'
        assert_postconf local_recipient_maps ''
        assert_postconf alias_maps ''
        assert_postconf alias_database ''
        assert_postconf virtual_alias_maps ''
        assert_postconf virtual_mailbox_maps ''
        assert_postconf myhostname '{{ forwardemail_alert_hostname }}'
        assert_postconf smtp_helo_name '$myhostname'
        assert_postconf smtp_address_preference 'ipv4'
        assert_postconf smtp_tls_security_level 'may'
        assert_postconf smtp_tls_CAfile \
          '/etc/ssl/certs/ca-certificates.crt'
        assert_postconf smtpd_relay_restrictions \
          'reject_unauth_destination'
        assert_postconf smtpd_recipient_restrictions \
          'reject_unauth_destination'
        assert_postconf sender_canonical_maps \
          'static:{{ alert_email_sender }}'
        assert_postconf sender_canonical_classes \
          'envelope_sender, header_sender'
      args:
        executable: /bin/bash
      changed_when: false

    # A previously rendered main.cf queues no handler, but an older master
    # process can still own sockets from its pre-hardening configuration.
    - name: Inspect Postfix runtime for stale TCP listeners
      shell: |
        set -o pipefail
        if ss -H -ltnp | \
          grep -Eq 'users:\(\("(master|smtpd|postscreen|smtp-sink)"'; then
          exit 0
        fi
        exit 1
      args:
        executable: /bin/bash
      register: postfix_runtime_listener_probe
      changed_when: false
      failed_when: postfix_runtime_listener_probe.rc not in [0, 1]

    - name: Restart Postfix to reconcile stale TCP listeners
      service:
        name: postfix
        state: restarted
      when: postfix_runtime_listener_probe.rc == 0

    - name: Verify Postfix exposes no TCP listening sockets
      shell: |
        set -o pipefail
        listeners="$(ss -H -ltnp)"
        if printf '%s\n' "$listeners" | \
          grep -Eq 'users:\(\("(master|smtpd|postscreen|smtp-sink)"'; then
          printf '%s\n' "$listeners" >&2
          exit 1
        fi
      args:
        executable: /bin/bash
      changed_when: false

    # Direct delivery only passes DMARC through SPF when the real egress IP is
    # authorized for the aligned envelope sender.  The transport is hardened
    # before these fail-closed DNS gates run, so a DNS error cannot leave the
    # package's temporary installation-time listener behind.
    - name: Verify alert sender SPF authorization for this host
      command:
        argv:
          - /usr/bin/spfquery.mail-spf-perl
          - --scope
          - mfrom
          - --identity
          - "{{ alert_email_sender }}"
          - --ip-address
          - "{{ forwardemail_alert_public_ip }}"
          - --helo-identity
          - "{{ forwardemail_alert_hostname }}"
      register: alert_sender_spf_check
      changed_when: false
      failed_when: alert_sender_spf_check.rc != 0
      tags:
        - forwardemail-alert-spf

    # Receivers evaluate HELO SPF when the envelope sender is empty (for
    # example, delivery-status notifications), so the hostname needs its own
    # passing policy in addition to the aligned envelope-sender policy.
    - name: Verify alert hostname SPF authorization for this host
      command:
        argv:
          - /usr/bin/spfquery.mail-spf-perl
          - --scope
          - helo
          - --identity
          - "{{ forwardemail_alert_hostname }}"
          - --ip-address
          - "{{ forwardemail_alert_public_ip }}"
      register: alert_hostname_spf_check
      changed_when: false
      failed_when: alert_hostname_spf_check.rc != 0
      tags:
        - forwardemail-alert-spf

    # Configure email for every genuine systemd failure. Optional Twilio SMS
    # is additionally allowed only for validated MongoDB, Logs, Redis, and PM2
    # systemd units on their exact configured hosts.
    - name: Install infrastructure alert sender dependencies
      ansible.builtin.apt:
        name:
          - ca-certificates
          - coreutils
          - curl
          - jq
          - util-linux
        state: present
        update_cache: true
      tags:
        - forwardemail-alert-policy

    - name: Count configured Twilio environment variables
      ansible.builtin.set_fact:
        twilio_values_present: >-
          {{
            [
              twilio_account_sid,
              twilio_auth_token,
              twilio_from_number,
              twilio_to_number
            ]
            | reject('equalto', '')
            | list
            | length
          }}
      no_log: true
      tags:
        - forwardemail-alert-policy

    - name: Validate Twilio SMS configuration
      ansible.builtin.assert:
        that:
          - (twilio_values_present | int) in [0, 4]
          - >-
            (twilio_values_present | int == 0) or
            (twilio_account_sid is match('^AC[0-9A-Fa-f]{32}$'))
          - >-
            (twilio_values_present | int == 0) or
            (twilio_auth_token is match('^[A-Za-z0-9]{32,64}$'))
          - >-
            (twilio_values_present | int == 0) or
            (twilio_from_number is match('^\\+[1-9][0-9]{7,14}$'))
          - >-
            (twilio_values_present | int == 0) or
            (twilio_to_number is match('^\\+[1-9][0-9]{7,14}$'))
        fail_msg: >-
          Set all four valid Twilio variables or leave all four unset:
          TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_FROM_NUMBER,
          and TWILIO_TO_NUMBER.
      no_log: true
      tags:
        - forwardemail-alert-policy

    - name: Derive private systemd SMS enabled state
      ansible.builtin.set_fact:
        forwardemail_systemd_sms_enabled: >-
          {{
            (twilio_values_present | int == 4) and
            (forwardemail_sms_services | length > 0)
          }}
      no_log: true
      tags:
        - forwardemail-alert-policy

    - name: Validate systemd status incident configuration
      ansible.builtin.assert:
        that:
          - systemd_incident_failure_threshold | int >= 2
          - systemd_incident_failure_threshold | int <= 10
          - systemd_incident_recovery_threshold | int >= 2
          - systemd_incident_recovery_threshold | int <= 10
          - >-
            (github_octokit_token | length == 0) or
            (github_octokit_token is match('^[A-Za-z0-9_]{20,255}$'))
          - >-
            (forwardemail_systemd_incident_config | length == 0) or
            (forwardemail_systemd_incident_component is
            match('^(mongo|logs|redis|bree|api|caldav|carddav|imap|mx1|mx2|pop3|smtp|sqlite|forwardemail)\\.forwardemail\\.net$|^forwardemail\\.net$'))
          - >-
            (forwardemail_systemd_incident_config | length == 0) or
            (forwardemail_systemd_incident_label is
            match('^[a-z0-9-]+$'))
          - >-
            (forwardemail_systemd_incident_config | length == 0) or
            ((forwardemail_systemd_incident_units | type_debug) == 'list')
          - >-
            (forwardemail_systemd_incident_config | length == 0) or
            ((forwardemail_systemd_incident_units | length) > 0)
          - >-
            (forwardemail_systemd_incident_config | length == 0) or
            ((forwardemail_systemd_incident_units | difference([
              'mongod.service',
              'valkey-server.service',
              'pm2-deploy.service',
              'pm2-health-check.service'
            ]) | length) == 0)
          - >-
            (forwardemail_systemd_incident_config | length == 0) or
            (forwardemail_systemd_incident_mapping_key in [
              'mongo.forwardemail.net:mongo-forwardemail-net:mongod.service',
              'logs.forwardemail.net:logs-forwardemail-net:mongod.service',
              'redis.forwardemail.net:redis-forwardemail-net:valkey-server.service',
              'bree.forwardemail.net:bree-forwardemail-net:pm2-deploy.service:pm2-health-check.service',
              'forwardemail.net:forwardemail-net-443-i-pv4:pm2-deploy.service:pm2-health-check.service',
              'api.forwardemail.net:api-forwardemail-net-443-i-pv4:pm2-deploy.service:pm2-health-check.service',
              'caldav.forwardemail.net:caldav-forwardemail-net-443-i-pv4:pm2-deploy.service:pm2-health-check.service',
              'carddav.forwardemail.net:carddav-forwardemail-net-443-i-pv4:pm2-deploy.service:pm2-health-check.service',
              'imap.forwardemail.net:imap-forwardemail-net-993-i-pv4:pm2-deploy.service:pm2-health-check.service',
              'mx1.forwardemail.net:mx1-forwardemail-net-25-i-pv4:pm2-deploy.service:pm2-health-check.service',
              'mx2.forwardemail.net:mx2-forwardemail-net-25-i-pv4:pm2-deploy.service:pm2-health-check.service',
              'pop3.forwardemail.net:pop3-forwardemail-net-995-i-pv4:pm2-deploy.service:pm2-health-check.service',
              'smtp.forwardemail.net:smtp-forwardemail-net-587-i-pv4:pm2-deploy.service:pm2-health-check.service',
              'sqlite.forwardemail.net:sqlite-forwardemail-net:pm2-deploy.service:pm2-health-check.service'
            ])
        fail_msg: >-
          Invalid systemd incident configuration. Use a valid
          GITHUB_OCTOKIT_TOKEN and thresholds from 2 through 10; only the
          fixed database and PM2 host/unit mappings are allowed.
      no_log: true
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Report email-only alert mode
      ansible.builtin.debug:
        msg: >-
          Twilio variables are unset; every genuine systemd failure and routine
          alert will continue by email only to {{ alert_email_recipients }}.
      when: twilio_values_present | int == 0

    - name: Report email-only non-eligible systemd mode
      ansible.builtin.debug:
        msg: >-
          This host has no approved MongoDB, Logs, Redis, or PM2 systemd SMS
          units. Every genuine systemd failure remains email-enabled, while SMS
          is disabled.
      when:
        - twilio_values_present | int == 4
        - forwardemail_sms_services | length == 0

    - name: Report disabled public systemd incident mode
      ansible.builtin.debug:
        msg: >-
          Public systemd incident reporting is disabled for
          {{ forwardemail_systemd_incident_component }} because
          GITHUB_OCTOKIT_TOKEN is unset; private failure notifications remain
          enabled.
      when:
        - forwardemail_systemd_incident_config | length > 0
        - not (forwardemail_systemd_incident_enabled | bool)
      tags:
        - forwardemail-systemd-incident

    - name: Disable notifications while updating alert policy
      ansible.builtin.command:
        argv:
          - systemctl
          - mask
          - --runtime
          - failure-notification@.service
      changed_when: false
      failed_when: false
      tags:
        - forwardemail-alert-policy

    - name: Remove old alert policy marker
      ansible.builtin.file:
        path: /etc/forwardemail-alerts/systemd-notifier-policy
        state: absent
      tags:
        - forwardemail-alert-policy

    - name: Create root-only infrastructure alert configuration directory
      ansible.builtin.file:
        path: /etc/forwardemail-alerts
        state: directory
        owner: root
        group: root
        mode: '0700'

    - name: Deploy root-only systemd incident environment
      ansible.builtin.template:
        src: >-
          {{ playbook_dir }}/templates/forwardemail-systemd-incident.env.j2
        dest: /etc/forwardemail-alerts/github-systemd-incident.env
        owner: root
        group: root
        mode: '0600'
      when: forwardemail_systemd_incident_enabled | bool
      no_log: true
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Remove systemd incident environment when reporting is disabled
      ansible.builtin.file:
        path: /etc/forwardemail-alerts/github-systemd-incident.env
        state: absent
      when: not (forwardemail_systemd_incident_enabled | bool)
      no_log: true
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Deploy Twilio environment on eligible SMS hosts
      ansible.builtin.template:
        src: "{{ playbook_dir }}/templates/forwardemail-alerts-twilio.env.j2"
        dest: /etc/forwardemail-alerts/twilio.env
        owner: root
        group: root
        mode: '0600'
      when: forwardemail_systemd_sms_enabled | bool
      no_log: true
      tags:
        - forwardemail-alert-policy

    - name: Remove Twilio environment from email-only hosts
      ansible.builtin.file:
        path: /etc/forwardemail-alerts/twilio.env
        state: absent
      when: not (forwardemail_systemd_sms_enabled | bool)
      tags:
        - forwardemail-alert-policy

    - name: Deploy email-only rate-limited alert sender
      ansible.builtin.template:
        src: "{{ playbook_dir }}/templates/send-rate-limited-email.sh.j2"
        dest: /usr/local/bin/send-rate-limited-email.sh
        owner: root
        group: root
        mode: '0755'
      tags:
        - forwardemail-alert-policy

    - name: Create root-only alert cooldown state directory
      ansible.builtin.file:
        path: /var/lib/forwardemail-alerts
        state: directory
        owner: root
        group: root
        mode: '0700'
      tags:
        - forwardemail-alert-policy

    - name: Deploy failure notification script
      ansible.builtin.template:
        src: "{{ playbook_dir }}/templates/send-failure-notification.sh.j2"
        dest: /usr/local/bin/send-failure-notification.sh
        owner: root
        group: root
        mode: '0755'
      tags:
        - forwardemail-alert-policy

    - name: Deploy privacy-preserving systemd incident reporter
      ansible.builtin.template:
        src: >-
          {{ playbook_dir }}/templates/report-systemd-status-incident.sh.j2
        dest: /usr/local/bin/report-systemd-status-incident.sh
        owner: root
        group: root
        mode: '0755'
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Deploy systemd failure notification service template
      ansible.builtin.template:
        src: "{{ playbook_dir }}/templates/failure-notification@.service.j2"
        dest: /etc/systemd/system/failure-notification@.service
        owner: root
        group: root
        mode: '0644'
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Deploy systemd incident event service template
      ansible.builtin.template:
        src: >-
          {{ playbook_dir }}/templates/forwardemail-systemd-incident-event@.service.j2
        dest: /etc/systemd/system/forwardemail-systemd-incident-event@.service
        owner: root
        group: root
        mode: '0644'
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Deploy systemd incident reconciliation service
      ansible.builtin.template:
        src: >-
          {{ playbook_dir }}/templates/forwardemail-systemd-incident-reconcile.service.j2
        dest: >-
          /etc/systemd/system/forwardemail-systemd-incident-reconcile.service
        owner: root
        group: root
        mode: '0644'
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Deploy systemd incident reconciliation timer
      ansible.builtin.template:
        src: >-
          {{ playbook_dir }}/templates/forwardemail-systemd-incident-reconcile.timer.j2
        dest: >-
          /etc/systemd/system/forwardemail-systemd-incident-reconcile.timer
        owner: root
        group: root
        mode: '0644'
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Create fleet-wide systemd failure-notification drop-in directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        owner: root
        group: root
        mode: '0755'
      loop:
        - /etc/systemd/system/service.d
        - /etc/systemd/system/failure-notification@.service.d
        - /etc/systemd/system/forwardemail-systemd-incident-event@.service.d
        - /etc/systemd/system/forwardemail-systemd-incident-reconcile.service.d
      tags:
        - forwardemail-alert-policy

    - name: Hook every systemd service failure into centralized notifications
      ansible.builtin.copy:
        dest: /etc/systemd/system/service.d/10-forwardemail-failure-notification.conf
        content: |
          # WARNING: This file is managed by Ansible
          [Unit]
          OnFailure=failure-notification@%n.service
        owner: root
        group: root
        mode: '0644'
      tags:
        - forwardemail-alert-policy

    - name: Prevent recursive failure notifications from alert services
      ansible.builtin.copy:
        dest: "{{ item }}/99-no-recursion.conf"
        content: |
          # WARNING: This file is managed by Ansible
          [Unit]
          OnFailure=
        owner: root
        group: root
        mode: '0644'
      loop:
        - /etc/systemd/system/failure-notification@.service.d
        - /etc/systemd/system/forwardemail-systemd-incident-event@.service.d
        - /etc/systemd/system/forwardemail-systemd-incident-reconcile.service.d
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Reload systemd daemon for fleet-wide failure notifications
      ansible.builtin.systemd_service:
        daemon_reload: true
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Verify current failure notification unit
      ansible.builtin.command:
        argv:
          - grep
          - -Fqx
          - ConditionPathExists=/etc/forwardemail-alerts/systemd-notifier-policy
          - /etc/systemd/system/failure-notification@.service
      changed_when: false
      tags:
        - forwardemail-alert-policy

    - name: Verify legacy detailed SMS body is absent
      ansible.builtin.command:
        argv:
          - grep
          - -F
          - Status=$sms_status
          - /usr/local/bin/send-failure-notification.sh
      register: forwardemail_legacy_sms_body
      changed_when: false
      failed_when: false
      tags:
        - forwardemail-alert-policy

    - name: Refuse to activate legacy failure notifier
      ansible.builtin.assert:
        that:
          - forwardemail_legacy_sms_body.rc != 0
        fail_msg: >-
          The installed failure notifier still contains the old detailed SMS
          body. It remains disabled; rerun the alert policy rollout.
      tags:
        - forwardemail-alert-policy

    - name: Mark current failure notification policy ready
      ansible.builtin.copy:
        dest: /etc/forwardemail-alerts/systemd-notifier-policy
        content: "managed by Ansible\n"
        owner: root
        group: root
        mode: '0600'
      tags:
        - forwardemail-alert-policy

    - name: Re-enable updated failure notifications
      ansible.builtin.command:
        argv:
          - systemctl
          - unmask
          - --runtime
          - failure-notification@.service
      changed_when: false
      tags:
        - forwardemail-alert-policy

    - name: Enable systemd incident reconciliation timer
      ansible.builtin.systemd:
        name: forwardemail-systemd-incident-reconcile.timer
        enabled: true
        state: started
      when: forwardemail_systemd_incident_enabled | bool
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    - name: Disable systemd incident reconciliation timer when unconfigured
      ansible.builtin.systemd:
        name: forwardemail-systemd-incident-reconcile.timer
        enabled: false
        state: stopped
      when: not (forwardemail_systemd_incident_enabled | bool)
      failed_when: false
      tags:
        - forwardemail-systemd-incident
        - forwardemail-alert-policy

    # Deploy security monitoring scripts
    - name: Create security monitor configuration directory
      file:
        path: /etc/security-monitor
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Create authorized IPs whitelist file
      copy:
        dest: /etc/security-monitor/authorized-ips.conf
        content: |
          # Authorized IP addresses (one per line)
          # Example: 192.168.1.100
        mode: '0644'
        owner: root
        group: root
        force: no

    - name: Create authorized users whitelist file
      copy:
        dest: /etc/security-monitor/authorized-users.conf
        content: |
          # Authorized users (one per line)
          devops
          deploy
        mode: '0644'
        owner: root
        group: root
        force: no

    - name: Create authorized USB devices whitelist file
      copy:
        dest: /etc/security-monitor/authorized-usb-devices.conf
        content: |
          # Authorized USB devices (vendor:product format, one per line)
          # Example: 1234:5678
          # Get IDs using: lsusb
        mode: '0644'
        owner: root
        group: root
        force: no

    - name: Create authorized root users whitelist file
      copy:
        dest: /etc/security-monitor/authorized-root-users.conf
        content: |
          # Authorized root users (one per line)
          devops
        mode: '0644'
        owner: root
        group: root
        force: no

    - name: Create authorized sudo users whitelist file
      copy:
        dest: /etc/security-monitor/authorized-sudo-users.conf
        content: |
          # Authorized sudo users (one per line)
          devops
        mode: '0644'
        owner: root
        group: root
        force: no

    # Create state directories for monitoring systems
    - name: Create Lynis audit monitor state directory
      file:
        path: /var/lib/lynis-audit-monitor
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Create package monitor state directory
      file:
        path: /var/lib/package-monitor
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Create open ports monitor state directory
      file:
        path: /var/lib/open-ports-monitor
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Validate outbound traffic monitor configuration
      ansible.builtin.assert:
        that:
          - outbound_traffic_monitor_threshold_bps | int >= 1000000
          - outbound_traffic_monitor_interval_seconds | int >= 1
          - outbound_traffic_monitor_interval_seconds | int <= 300
          - outbound_traffic_monitor_consecutive_breaches | int >= 1
          - outbound_traffic_monitor_consecutive_breaches | int <= 60
          - outbound_traffic_monitor_top_lines | int >= 1
          - outbound_traffic_monitor_top_lines | int <= 100
        fail_msg: >-
          Invalid outbound traffic monitor configuration. The threshold must be
          at least 1 Mbps; the sample interval must be 1-300 seconds; the
          consecutive breach count must be 1-60; and TOP_LINES must be 1-100.
      when: outbound_traffic_monitor_enabled | bool
      tags:
        - forwardemail-outbound-traffic-monitor

    - name: Install outbound traffic monitor runtime dependencies
      ansible.builtin.apt:
        name:
          - iproute2
          - procps
        state: present
      when: outbound_traffic_monitor_enabled | bool
      tags:
        - forwardemail-outbound-traffic-monitor

    - name: Create outbound traffic monitor state directory
      ansible.builtin.file:
        path: /var/lib/outbound-traffic-monitor
        state: directory
        owner: root
        group: root
        mode: '0700'
      when: outbound_traffic_monitor_enabled | bool
      tags:
        - forwardemail-outbound-traffic-monitor

    - name: Deploy outbound traffic monitor script
      ansible.builtin.copy:
        src: "{{ playbook_dir }}/files/outbound-traffic-monitor.sh"
        dest: /usr/local/bin/outbound-traffic-monitor.sh
        owner: root
        group: root
        mode: '0755'
      when: outbound_traffic_monitor_enabled | bool
      notify: Restart monitoring services
      tags:
        - forwardemail-outbound-traffic-monitor

    - name: Deploy outbound traffic monitor configuration
      ansible.builtin.template:
        src: "{{ playbook_dir }}/templates/outbound-traffic-monitor.conf.j2"
        dest: /etc/security-monitor/outbound-traffic-monitor.conf
        owner: root
        group: root
        mode: '0644'
      when: outbound_traffic_monitor_enabled | bool
      notify: Restart monitoring services
      tags:
        - forwardemail-outbound-traffic-monitor

    - name: Deploy outbound traffic monitor service
      ansible.builtin.template:
        src: "{{ playbook_dir }}/templates/outbound-traffic-monitor.service.j2"
        dest: /etc/systemd/system/outbound-traffic-monitor.service
        owner: root
        group: root
        mode: '0644'
      when: outbound_traffic_monitor_enabled | bool
      notify: Restart monitoring services
      tags:
        - forwardemail-outbound-traffic-monitor

    - name: Deploy outbound traffic monitor timer
      ansible.builtin.template:
        src: "{{ playbook_dir }}/templates/outbound-traffic-monitor.timer.j2"
        dest: /etc/systemd/system/outbound-traffic-monitor.timer
        owner: root
        group: root
        mode: '0644'
      when: outbound_traffic_monitor_enabled | bool
      notify: Restart monitoring services
      tags:
        - forwardemail-outbound-traffic-monitor

    - name: Disable outbound traffic monitoring when configured off
      ansible.builtin.systemd:
        name: outbound-traffic-monitor.timer
        enabled: false
        state: stopped
      when: not (outbound_traffic_monitor_enabled | bool)
      failed_when: false
      tags:
        - forwardemail-outbound-traffic-monitor

    - name: Deploy system resource monitor script
      copy:
        src: "{{ playbook_dir }}/files/system-resource-monitor.sh"
        dest: /usr/local/bin/system-resource-monitor.sh
        owner: root
        group: root
        mode: '0755'

    - name: Deploy SSH security monitor script
      copy:
        src: "{{ playbook_dir }}/files/ssh-security-monitor.sh"
        dest: /usr/local/bin/ssh-security-monitor.sh
        owner: root
        group: root
        mode: '0755'

    - name: Deploy USB device monitor script
      copy:
        src: "{{ playbook_dir }}/files/usb-device-monitor.sh"
        dest: /usr/local/bin/usb-device-monitor.sh
        owner: root
        group: root
        mode: '0755'

    - name: Deploy root access monitor script
      copy:
        src: "{{ playbook_dir }}/files/root-access-monitor.sh"
        dest: /usr/local/bin/root-access-monitor.sh
        owner: root
        group: root
        mode: '0755'

    - name: Deploy Lynis audit monitor script
      copy:
        src: "{{ playbook_dir }}/files/lynis-audit-monitor.sh"
        dest: /usr/local/bin/lynis-audit-monitor.sh
        owner: root
        group: root
        mode: '0755'

    - name: Deploy package monitor script
      copy:
        src: "{{ playbook_dir }}/files/package-monitor.sh"
        dest: /usr/local/bin/package-monitor.sh
        owner: root
        group: root
        mode: '0755'

    - name: Deploy open ports monitor script
      copy:
        src: "{{ playbook_dir }}/files/open-ports-monitor.sh"
        dest: /usr/local/bin/open-ports-monitor.sh
        owner: root
        group: root
        mode: '0755'

    - name: Deploy SSL certificate monitor script
      copy:
        src: "{{ playbook_dir }}/files/ssl-certificate-monitor.sh"
        dest: /usr/local/bin/ssl-certificate-monitor.sh
        owner: root
        group: root
        mode: '0755'

    # Deploy systemd service units
    - name: Deploy system resource monitor service
      template:
        src: "{{ playbook_dir }}/templates/system-resource-monitor.service.j2"
        dest: /etc/systemd/system/system-resource-monitor.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart monitoring services

    - name: Deploy system resource monitor timer
      template:
        src: "{{ playbook_dir }}/templates/system-resource-monitor.timer.j2"
        dest: /etc/systemd/system/system-resource-monitor.timer
        owner: root
        group: root
        mode: '0644'

    - name: Deploy SSH security monitor service
      template:
        src: "{{ playbook_dir }}/templates/ssh-security-monitor.service.j2"
        dest: /etc/systemd/system/ssh-security-monitor.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart monitoring services

    - name: Deploy SSH security monitor timer
      template:
        src: "{{ playbook_dir }}/templates/ssh-security-monitor.timer.j2"
        dest: /etc/systemd/system/ssh-security-monitor.timer
        owner: root
        group: root
        mode: '0644'

    - name: Deploy USB device monitor service
      template:
        src: "{{ playbook_dir }}/templates/usb-device-monitor.service.j2"
        dest: /etc/systemd/system/usb-device-monitor.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart monitoring services

    - name: Deploy USB device monitor timer
      template:
        src: "{{ playbook_dir }}/templates/usb-device-monitor.timer.j2"
        dest: /etc/systemd/system/usb-device-monitor.timer
        owner: root
        group: root
        mode: '0644'

    - name: Deploy USB device monitor udev rules
      template:
        src: "{{ playbook_dir }}/templates/usb-monitor.rules.j2"
        dest: /etc/udev/rules.d/99-usb-monitor.rules
        owner: root
        group: root
        mode: '0644'

    - name: Populate authorized USB devices from environment variable
      copy:
        content: |
          {% set usb_devices = lookup('env', 'USB_DEVICES_PERMITTED') | default('') %}
          {% if usb_devices %}
          {% for device in usb_devices.split(',') %}
          {{ device.strip() }}
          {% endfor %}
          {% endif %}
        dest: /etc/security-monitor/authorized-usb-devices.conf
        owner: root
        group: root
        mode: '0644'

    - name: Deploy root access monitor service
      template:
        src: "{{ playbook_dir }}/templates/root-access-monitor.service.j2"
        dest: /etc/systemd/system/root-access-monitor.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart monitoring services

    - name: Deploy root access monitor timer
      template:
        src: "{{ playbook_dir }}/templates/root-access-monitor.timer.j2"
        dest: /etc/systemd/system/root-access-monitor.timer
        owner: root
        group: root
        mode: '0644'

    - name: Deploy Lynis audit monitor service
      template:
        src: "{{ playbook_dir }}/templates/lynis-audit-monitor.service.j2"
        dest: /etc/systemd/system/lynis-audit-monitor.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart monitoring services

    - name: Deploy Lynis audit monitor timer
      template:
        src: "{{ playbook_dir }}/templates/lynis-audit-monitor.timer.j2"
        dest: /etc/systemd/system/lynis-audit-monitor.timer
        owner: root
        group: root
        mode: '0644'

    - name: Deploy package monitor service
      template:
        src: "{{ playbook_dir }}/templates/package-monitor.service.j2"
        dest: /etc/systemd/system/package-monitor.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart monitoring services

    - name: Deploy package monitor timer
      template:
        src: "{{ playbook_dir }}/templates/package-monitor.timer.j2"
        dest: /etc/systemd/system/package-monitor.timer
        owner: root
        group: root
        mode: '0644'

    - name: Deploy open ports monitor service
      template:
        src: "{{ playbook_dir }}/templates/open-ports-monitor.service.j2"
        dest: /etc/systemd/system/open-ports-monitor.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart monitoring services

    - name: Deploy open ports monitor timer
      template:
        src: "{{ playbook_dir }}/templates/open-ports-monitor.timer.j2"
        dest: /etc/systemd/system/open-ports-monitor.timer
        owner: root
        group: root
        mode: '0644'

    - name: Deploy SSL certificate monitor service
      template:
        src: "{{ playbook_dir }}/templates/ssl-certificate-monitor.service.j2"
        dest: /etc/systemd/system/ssl-certificate-monitor.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart monitoring services

    - name: Deploy SSL certificate monitor timer
      template:
        src: "{{ playbook_dir }}/templates/ssl-certificate-monitor.timer.j2"
        dest: /etc/systemd/system/ssl-certificate-monitor.timer
        owner: root
        group: root
        mode: '0644'

    # Reload systemd and enable monitoring services
    - name: Reload systemd daemon for monitoring services
      systemd:
        daemon_reload: yes

    - name: Enable and start outbound traffic monitor timer
      ansible.builtin.systemd:
        name: outbound-traffic-monitor.timer
        enabled: true
        state: started
      when: outbound_traffic_monitor_enabled | bool
      tags:
        - forwardemail-outbound-traffic-monitor

    - name: Enable and start system resource monitor timer
      systemd:
        name: system-resource-monitor.timer
        enabled: yes
        state: started

    - name: Enable and start SSH security monitor timer
      systemd:
        name: ssh-security-monitor.timer
        enabled: yes
        state: started

    - name: Enable and start USB device monitor timer
      systemd:
        name: usb-device-monitor.timer
        enabled: yes
        state: started

    - name: Enable and start root access monitor timer
      systemd:
        name: root-access-monitor.timer
        enabled: yes
        state: started

    - name: Enable and start Lynis audit monitor timer
      systemd:
        name: lynis-audit-monitor.timer
        enabled: yes
        state: started

    - name: Enable and start package monitor timer
      systemd:
        name: package-monitor.timer
        enabled: yes
        state: started

    - name: Enable and start open ports monitor timer
      systemd:
        name: open-ports-monitor.timer
        enabled: yes
        state: started

    - name: Enable and start SSL certificate monitor timer
      systemd:
        name: ssl-certificate-monitor.timer
        enabled: yes
        state: started

    - name: Reload udev rules for USB monitoring
      command: udevadm control --reload-rules

    - name: Trigger udev rules
      command: udevadm trigger

    # Configure comprehensive command logging
    - name: Install auditd for command logging
      apt:
        name: auditd
        state: present
        update_cache: yes

    - name: Deploy audit rules for command logging
      copy:
        src: "{{ playbook_dir }}/files/audit-commands.rules"
        dest: /etc/audit/rules.d/audit-commands.rules
        owner: root
        group: root
        mode: '0640'
      notify: Reload auditd rules

    # Deploy enhanced bash/zsh command logging to multiple locations
    # This ensures ALL commands are logged regardless of shell type or mode:
    # - /etc/profile.d/ for login shells
    # - /etc/bash.bashrc for all interactive bash shells
    # - /etc/zsh/zshrc for all zsh shells
    # - SSH ForceCommand wrapper for non-interactive SSH commands

    - name: Deploy enhanced bash command logging to profile.d (login shells)
      copy:
        src: "{{ playbook_dir }}/files/bash-logging-enhanced.sh"
        dest: /etc/profile.d/bash-logging.sh
        owner: root
        group: root
        mode: '0644'

    - name: Deploy enhanced bash command logging to bash.bashrc (all bash shells)
      blockinfile:
        path: /etc/bash.bashrc
        marker: "# {mark} ANSIBLE MANAGED BLOCK - Command Logging"
        insertbefore: '^\[ -z "\$PS1" \] && return'
        block: |
          # Source bash command logging for ALL shells (interactive and non-interactive)
          if [ -f /etc/profile.d/bash-logging.sh ]; then
              source /etc/profile.d/bash-logging.sh
          fi
        owner: root
        group: root
        mode: '0644'

    - name: Create zsh configuration directory
      file:
        path: /etc/zsh/zshrc.d
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Deploy enhanced zsh command logging configuration
      copy:
        src: "{{ playbook_dir }}/files/bash-logging-enhanced.sh"
        dest: /etc/zsh/zshrc.d/command-logging.zsh
        owner: root
        group: root
        mode: '0644'

    - name: Ensure zshrc sources zshrc.d directory
      blockinfile:
        path: /etc/zsh/zshrc
        create: yes
        marker: "# {mark} ANSIBLE MANAGED BLOCK - Command Logging"
        block: |
          # Source command logging
          if [ -d /etc/zsh/zshrc.d ]; then
            for file in /etc/zsh/zshrc.d/*.zsh; do
              [ -r "$file" ] && source "$file"
            done
          fi
        owner: root
        group: root
        mode: '0644'

    - name: Configure rsyslog to capture bash commands
      blockinfile:
        path: /etc/rsyslog.d/50-bash-logging.conf
        create: yes
        block: |
          # Log bash commands to separate file
          if $programname == 'bash_command' or $programname == 'zsh_command' then {
              action(type="omfile" file="/var/log/bash-commands.log" FileOwner="syslog" FileGroup="adm" FileCreateMode="0640")
              stop
          }
        owner: root
        group: root
        mode: '0644'
      notify: Restart rsyslog

    - name: Create bash commands log file
      file:
        path: /var/log/bash-commands.log
        state: touch
        owner: syslog
        group: adm
        mode: '0640'

    - name: Configure logrotate for bash commands log
      copy:
        dest: /etc/logrotate.d/bash-commands
        content: |
          /var/log/bash-commands.log {
              daily
              rotate 30
              compress
              delaycompress
              missingok
              notifempty
              create 0640 syslog adm
              sharedscripts
              postrotate
                  /usr/lib/rsyslog/rsyslog-rotate
              endscript
          }
        owner: root
        group: root
        mode: '0644'

    # create devops group
    - name: Create devops group
      group:
        name: devops
        state: present

    - name: Set devops group to have sudo access
      lineinfile:
        path: /etc/sudoers
        state: present
        regexp: ^%devops
        line: "%devops ALL=(ALL) NOPASSWD: ALL"
        validate: /usr/sbin/visudo -cf %s

    # create devops user (with sudo)
    - name: Create a devops user with sudo privileges
      user:
        name: devops
        password: "!"
        state: present
        groups: devops
        append: true
        create_home: true
        shell: /bin/bash

    - name: Set authorized key for devops user
      authorized_key:
        user: devops
        state: present
        key: "{{ copy_local_key }}"

    - name: Set authorized key 2 for devops user
      authorized_key:
        user: devops
        state: present
        key: "{{ copy_local_key_2 }}"

    # create deploy user
    - name: Create a deploy user
      user:
        name: deploy
        password: "!"
        state: present
        create_home: true
        shell: /bin/bash
        generate_ssh_key: true
        ssh_key_bits: 4096

    - name: Set authorized key for deploy user
      authorized_key:
        user: deploy
        state: present
        key: "{{ copy_local_key }}"

    - name: Set authorized key 2 for deploy user
      authorized_key:
        user: deploy
        state: present
        key: "{{ copy_local_key_2 }}"

    - name: Remove non-interactive from deploy bashrc
      ansible.builtin.replace:
        path: /home/deploy/.bashrc
        regexp: "{{ '      *) return;;' | regex_escape() }}"
        replace: "      *) :;"
      tags:
        - bash

    # disable root login
    - name: Disable root login
      lineinfile:
        path: /etc/ssh/sshd_config
        line: "{{ item.key }} {{ item.value }}"
        state: "{{ item.state }}"
      loop:
        - { key: PermitRootLogin, value: "yes", state: absent }
        - { key: PasswordAuthentication, value: "yes", state: absent }
        - { key: PermitRootLogin, value: "no", state: present }
        - { key: PasswordAuthentication, value: "no", state: present }

    # https://superuser.com/a/1578266
    - name: Disable root password
      user:
        name: root
        password_lock: true
      notify: Restart SSH

    # modify ulimit for devops and deploy
    # https://gist.github.com/visualskyrim/8d93a8be0a3ef6dd6598ec8550f6eadd#file-modify_ulimit-yml-L7
    - name: Configure system settings, file descriptors and number of threads
      pam_limits:
        domain: "*"
        limit_type: "{{ item.limit_type }}"
        limit_item: "{{ item.limit_item }}"
        value: "{{ item.value }}"
      with_items:
        - { limit_type: "-", limit_item: nofile, value: 65536 }
        - { limit_type: "-", limit_item: nproc, value: 65536 }
        - { limit_type: soft, limit_item: memlock, value: unlimited }
        - { limit_type: hard, limit_item: memlock, value: unlimited }
    - name: Reload settings from all system configuration files
      shell: sysctl --system

    # Silence dubious ownership git warning
    - name: Silence git safe dir warning
      become: true
      become_user: deploy
      shell: git config --global --add safe.directory '*'

- hosts: "{{ security_target_hosts | default('all') }}"
  name: Mongo Shell
  become: true
  become_user: root
  tasks:
    - name: Setup keys
      shell: |
        wget -qO - https://www.mongodb.org/static/pgp/server-6.0.asc | sudo apt-key add -
        echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu $(lsb_release -c -s)/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list
    - name: Install package
      apt:
        name:
          - mongodb-mongosh
        update_cache: true

    # Disable mongosh telemetry globally for all users
    - name: Create mongosh config directory for root
      file:
        path: /root/.mongodb/mongosh
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Disable mongosh telemetry for root
      copy:
        dest: /root/.mongodb/mongosh/config
        content: |
          {
            "enableTelemetry": false,
            "disableGreetingMessage": true
          }
        owner: root
        group: root
        mode: '0644'

    - name: Create mongosh config directory for deploy user
      file:
        path: /home/deploy/.mongodb/mongosh
        state: directory
        owner: deploy
        group: deploy
        mode: '0755'
      ignore_errors: true

    - name: Disable mongosh telemetry for deploy user
      copy:
        dest: /home/deploy/.mongodb/mongosh/config
        content: |
          {
            "enableTelemetry": false,
            "disableGreetingMessage": true
          }
        owner: deploy
        group: deploy
        mode: '0644'
      ignore_errors: true

    - name: Create mongosh config directory for devops user
      file:
        path: /home/devops/.mongodb/mongosh
        state: directory
        owner: devops
        group: devops
        mode: '0755'
      ignore_errors: true

    - name: Disable mongosh telemetry for devops user
      copy:
        dest: /home/devops/.mongodb/mongosh/config
        content: |
          {
            "enableTelemetry": false,
            "disableGreetingMessage": true
          }
        owner: devops
        group: devops
        mode: '0644'
      ignore_errors: true

- hosts: "{{ security_target_hosts | default('all') }}"
  name: Sysctl
  become: true
  become_user: root
  vars:
    #
    # kernel tuning and performance optimizations
    #
    # https://medium.com/@k1d_bl4ck/a-quick-story-about-node-js-socket-io-and-the-linux-tcp-stack-bf1e8318b20e
    # https://gist.github.com/vongosling/9929680
    # https://wiki.mikejung.biz/Sysctl_tweaks
    # https://docs.continuent.com/tungsten-clustering-6.1/performance-networking.html
    # https://www.vpndada.com/how-to-setup-a-shadowsocks-server-on-digitalocean/
    #
    # TODO: we should research more configuration settings from these links:
    #       https://udgwebdev.github.io/tunning-em-servidor-linux/
    #       https://gist.github.com/voluntas/bc54c60aaa7ad6856e6f6a928b79ab6c
    #       https://serverdiary.com/linux/how-to-mitigate-tcp-syn-flood-attack-and-resolve-it-on-linux/
    #
    sysctl_settings:
      # disable coredump
      - name: fs.suid_dumpable
        value: 0
      - name: kernel.core_pattern
        value: "|/bin/false"

      # Enable Randomized Layout of Virtual Address Space
      # https://docs.datadoghq.com/security/default_rules/xccdf-org-ssgproject-content-rule-sysctl-kernel-randomize-va-space/
      - name: kernel.randomize_va_space
        value: 2

      #
      # socat has IPv6 issues and is hard to configure
      # authbind et all also have their own issues and configuration pains
      # this is an extremely simple and straightforward way to allow port binding
      #
      # <https://ar.al/2022/08/30/dear-linux-privileged-ports-must-die/>
      # <https://stackoverflow.com/a/51439516>
      # <https://github.com/moby/moby/pull/41030/files#diff-9f91bff23e0bd70d6429b63d9db2d8180d2e89cdb64db4fb3e10a96f74d36271R792>
      #
      - name: net.ipv4.ip_unprivileged_port_start
        value: 25

      #
      # handle ufw forwarding
      #
      - name: net.ipv4.ip_forward
        value: 0
      - name: net.ipv6.conf.default.forwarding
        value: 0
      - name: net.ipv6.conf.all.forwarding
        value: 0

      # handle swapping idle processes to disk
      # https://medium.com/@sauravomar01/linux-kernel-tuning-and-performance-tweaks-d7848178aaa2
      - name: vm.swappiness
        value: 0
      - name: vm.dirty_ratio
        value: 60
      - name: vm.dirty_background_ratio
        value: 2
      - name: vm.vfs_cache_pressure
        value: 50

      # allow local port range
      - name: net.ipv4.ip_local_port_range
        value: 1024 65535

      #
      # increase amount of option memory buffers
      # Dynamic scaling based on RAM (used for ancillary data, MSG_ZEROCOPY, etc.)
      # Scales conservatively with available memory
      #
      - name: net.core.optmem_max
        value: >-
          {% if ansible_memtotal_mb >= 65536 %}
          134217728
          {% elif ansible_memtotal_mb >= 32768 %}
          67108864
          {% elif ansible_memtotal_mb >= 16384 %}
          33554432
          {% else %}
          25165824
          {% endif %}

      # max number of concurrently open files
      - name: fs.file-max
        value: 2097152

      # increase the max number of "backlogged sockets" (default: 128)
      # (max number of connections that can be queued for acceptance)
      # Increased to 16384 for high-traffic email server (IMAP, SMTP, POP3, WebSocket)
      - name: net.core.somaxconn
        value: 16384

      # length of time orphaned (unreferenced) connection will wait (default: 60)
      # Lowered to 20s for faster resource reclamation on high-traffic servers
      - name: net.ipv4.tcp_fin_timeout
        value: 20

      #
      # allow more aggressive network throughput
      # https://en.wikipedia.org/wiki/TCP_window_scale_option
      #
      - name: net.ipv4.tcp_window_scaling
        value: 1

      #
      # configure tcp keepalive
      # https://webhostinggeeks.com/howto/configure-linux-tcp-keepalive-setting/
      #
      - name: net.ipv4.tcp_keepalive_time
        value: 60
      - name: net.ipv4.tcp_keepalive_intvl
        value: 10
      #
      # NOTE: it seems like this was removed in newer kernels
      # `sysctl: cannot stat /proc/sys/net/ipv4/tcp_tw_recycle: No such file or directory`
      #
      # - name: net.ipv4.tcp_keepalive_probe
      # - value: 6

      # max remembered connection requests which did not yet receive ACK (default: 1024)
      # (how many half-open connections can be kept in the queue)
      # Should match or exceed net.core.somaxconn
      - name: net.ipv4.tcp_max_syn_backlog
        value: 16384

      # increase system ip port limts to allow for more connections
      - name: net.ipv4.ip_local_port_range
        value: 1024 65535

      # number of packets queued on INPUT (default: 1000)
      # Increased to handle high packet rates on email servers
      - name: net.core.netdev_max_backlog
        value: 16384

      #
      # Network congestion control and queue discipline
      # fq_codel is the Linux kernel default since 4.12 (2017) and optimal for CUBIC
      # Provides fair queueing + Active Queue Management (AQM) with CoDel
      # Best for mixed workloads, bufferbloat prevention, and reliability
      # https://fasterdata.es.net/host-tuning/linux/
      # Note: http.yml overrides this with BBR + fq for HTTP servers
      #
      - name: net.core.default_qdisc
        value: fq_codel
      - name: net.ipv4.tcp_congestion_control
        value: cubic
      - name: net.ipv4.tcp_notsent_lowat
        value: 16384

      # protect against tcp time-wait
      - name: net.ipv4.tcp_rfc1337
        value: 1

      #
      # number of sockets in the "time-wait" state allowed to exist (prevents simple DOS attacks)
      # https://easyengine.io/tutorials/linux/sysctl-conf/
      # https://docs.continuent.com/tungsten-clustering-5.4/performance-networking.html
      #
      - name: net.ipv4.tcp_max_tw_buckets
        value: 1440000

      #
      # we do not use this because it does not work well with load balancers
      # and it also was removed from linux in v4.12
      # https://stackoverflow.com/questions/6426253/tcp-tw-reuse-vs-tcp-tw-recycle-which-to-use-or-both
      #
      # NOTE: this was removed/deprecated in newer kernels
      # `sysctl: cannot stat /proc/sys/net/ipv4/tcp_tw_recycle: No such file or directory`
      #
      # - name: net.ipv4.tcp_tw_recycle
      #   value: 0

      # allow to reuse TIME_WAIT sockets for new connections when safe from protocol
      - name: net.ipv4.tcp_tw_reuse
        value: 1

      #
      # TCP receive buffer (min default max)
      # Dynamic scaling based on RAM for large email attachments and bulk operations
      # Conservative scaling to prevent excessive memory usage with many connections
      # https://fasterdata.es.net/host-tuning/linux/
      #
      - name: net.ipv4.tcp_rmem
        value: >-
          {% if ansible_memtotal_mb >= 65536 %}
          4096 131072 134217728
          {% elif ansible_memtotal_mb >= 32768 %}
          4096 87380 67108864
          {% elif ansible_memtotal_mb >= 16384 %}
          4096 87380 33554432
          {% else %}
          4096 87380 16777216
          {% endif %}

      #
      # Max TCP receive buffer per socket (must match or exceed tcp_rmem max)
      # Dynamic scaling based on RAM
      #
      - name: net.core.rmem_max
        value: >-
          {% if ansible_memtotal_mb >= 65536 %}
          134217728
          {% elif ansible_memtotal_mb >= 32768 %}
          67108864
          {% elif ansible_memtotal_mb >= 16384 %}
          33554432
          {% else %}
          16777216
          {% endif %}

      #
      # TCP send buffer (min default max)
      # Dynamic scaling based on RAM for large email attachments and bulk operations
      # Conservative scaling to prevent excessive memory usage with many connections
      # https://fasterdata.es.net/host-tuning/linux/
      #
      - name: net.ipv4.tcp_wmem
        value: >-
          {% if ansible_memtotal_mb >= 65536 %}
          4096 16384 134217728
          {% elif ansible_memtotal_mb >= 32768 %}
          4096 65536 67108864
          {% elif ansible_memtotal_mb >= 16384 %}
          4096 65536 33554432
          {% else %}
          4096 65536 16777216
          {% endif %}

      #
      # Max TCP send buffer per socket (must match or exceed tcp_wmem max)
      # Dynamic scaling based on RAM
      #
      - name: net.core.wmem_max
        value: >-
          {% if ansible_memtotal_mb >= 65536 %}
          134217728
          {% elif ansible_memtotal_mb >= 32768 %}
          67108864
          {% elif ansible_memtotal_mb >= 16384 %}
          33554432
          {% else %}
          16777216
          {% endif %}

      # provide RFC 2861 behavior and time out congestion window after an idle period
      # many suggest to disable it to improve performance in some cases
      - name: net.ipv4.tcp_slow_start_after_idle
        value: 0

      # disable caching of TCP congestion state
      - name: net.ipv4.tcp_no_metrics_save
        value: 1

      # set number of retries for for TCP 3 way handshake (default is 5)
      # https://www.justsomestuff.co.uk/wiki/doku.php/linux/syn_tcp_timeout
      - name: net.ipv4.tcp_syn_retries
        value: 3

      #
      # number of times SYNACKS for passive TCP connection are tried
      # https://blog.cloudflare.com/syn-packet-handling-in-the-wild/
      #
      - name: net.ipv4.tcp_synack_retries
        value: 2

      #
      # TCP Fast Open (TFO) - DISABLED
      # While TFO can reduce latency by 15-41%, it is NOT yet supported in Node.js
      # libuv PR #1136 (2016) is still open and not merged as of 2025
      # Node.js issue #8066 (2016) is still open with no implementation
      # https://github.com/nodejs/node/issues/8066
      # https://github.com/libuv/libuv/pull/1136
      #
      # - name: net.ipv4.tcp_fastopen
      #   value: 3

      #
      # tune ICMP black holes and adjust path MTU in a smart way
      # https://blog.cloudflare.com/path-mtu-discovery-in-practice/
      #
      - name: net.ipv4.tcp_mtu_probing
        value: 1
      - name: net.ipv4.tcp_base_mss
        value: 1024

      #
      # make the system resistant to out of memory scenarios
      # https://www.linbit.com/kernel-min_free_kbytes/
      # https://dom.as/2016/05/13/linux-memory-management-for-servers/
      #
      # Dynamic calculation based on total RAM (1.56% of RAM):
      # - 64+ GB RAM: 1 GB (1048576 KB) - Facebook's recommendation for >100GB
      # - 32-63 GB RAM: 512 MB (524288 KB) - recommended for high-traffic servers
      # - 16-31 GB RAM: 256 MB (262144 KB) - recommended for email/database servers
      # - 8-15 GB RAM: 128 MB (131072 KB) - previous default
      # - <8 GB RAM: 64 MB (65536 KB) - legacy default
      #
      # Why this matters: kernel triggers background reclamation at 25% of this value.
      # With 64 MB, background reclaim starts at only 16 MB, causing direct reclaim
      # stalls (multi-second freezes) on 16-32 GB servers under memory pressure.
      #
      - name: vm.min_free_kbytes
        value: >-
          {% if ansible_memtotal_mb >= 65536 %}
          1048576
          {% elif ansible_memtotal_mb >= 32768 %}
          524288
          {% elif ansible_memtotal_mb >= 16384 %}
          262144
          {% elif ansible_memtotal_mb >= 8192 %}
          131072
          {% else %}
          65536
          {% endif %}

      # control syncookies
      - name: net.ipv4.tcp_syncookies
        value: 1

      # enable timestamps as defined in RFC1323
      - name: net.ipv4.tcp_timestamps
        value: 1

      #
      # Max orphaned TCP sockets (not attached to any user file handle)
      # Orphaned connections consume memory, so scale with available RAM
      # Formula: RAM_MB / 16 (conservative scaling)
      # If exceeded, orphaned connections are reset and warning printed
      #
      - name: net.ipv4.tcp_max_orphans
        value: >-
          {% if ansible_memtotal_mb >= 65536 %}
          4096000
          {% elif ansible_memtotal_mb >= 32768 %}
          2048000
          {% elif ansible_memtotal_mb >= 16384 %}
          1024000
          {% else %}
          262144
          {% endif %}

  roles:
    # https://github.com/Oefenweb/ansible-sysctl
    - role: sysctl

    # https://github.com/Oefenweb/ansible-dns
    - role: dns
      dns_nameservers:
        # cloudflare and google (ipv4/ipv6 and rotated)
        - 1.1.1.1
        - 2606:4700:4700::1111
        - 1.0.0.1
        - 2606:4700:4700::1001
        - 8.8.8.8
        - 2001:4860:4860::8888
        - 8.8.4.4
        - 2001:4860:4860::8844

    # NOTE: NTP is now handled by chrony (imported via chrony-timesync.yml above)
    # The legacy oefenweb.ntp role (ntpd) has been removed because:
    #   - ntpd does not integrate with systemd/timedatectl (shows "NTP service: n/a")
    #   - chrony is the recommended NTP client for modern Ubuntu/Debian systems
    #   - ntpd and chrony conflict with each other (both bind to NTP ports)

    # https://github.com/Oefenweb/ansible-timezone
    #- role: timezone
    #  timezone_zone: 'XYZ'

    # https://github.com/Oefenweb/ansible-fail2ban
    - role: fail2ban
      fail2ban_maxretry: 2
      fail2ban_bantime: -1
      # dbpurgeage is ignored if bantime is -1 (?)
      fail2ban_dbpurgeage: 365d
      fail2ban_findtime: 365d
      # Email notifications for bans
      fail2ban_destemail: "{{ alert_email_recipients | default('security@forwardemail.net', true) }}"
      fail2ban_sendername: "{{ alert_email_sender | default('mailerdaemon@forwardemail.net', true) }}"
      fail2ban_sender: "{{ alert_email_sender | default('mailerdaemon@forwardemail.net', true) }}"
      fail2ban_action: "%(action_mwl)s"  # mail with logs
      fail2ban_mta: sendmail

      # TODO: add IMAP, POP3, and SMTP with pm2 error log output
      # https://www.teaparty.net/technotes/fail2ban.html#:~:text=I%20added%20the%20following%20to%20my%20jail.conf%3A

    # https://github.com/hifis-net/ansible-collection-toolkit (new)
    # https://github.com/jnv/ansible-role-unattended-upgrades (old)
    # - role: unattended-upgrades
    - role: hifis.toolkit.unattended_upgrades
      # 08:00 UTC = 3:00 AM CDT / 2:00 AM CST for non-database hosts
      unattended_automatic_reboot: "{{ false if ('mongo' in group_names or 'redis' in group_names or 'logs' in group_names) else true }}"
      unattended_automatic_reboot_time: "08:00"
      unattended_automatic_reboot_with_users: false
      # Email notifications for updates
      unattended_mail: "{{ alert_email_recipients | default('security@forwardemail.net', true) }}"
      unattended_mail_sender: "{{ alert_email_sender | default('mailerdaemon@forwardemail.net', true) }}"
      unattended_mail_report: "on-change"
      tags:
        - forwardemail-alert-policy
