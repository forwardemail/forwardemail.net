# Managed by Ansible.  Root-only credentials for infrastructure SMS alerts.
TWILIO_ACCOUNT_SID={{ twilio_account_sid | quote }}
TWILIO_AUTH_TOKEN={{ twilio_auth_token | quote }}
TWILIO_FROM_NUMBER={{ twilio_from_number | quote }}
TWILIO_TO_NUMBER={{ twilio_to_number | quote }}
