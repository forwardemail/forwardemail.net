Included assets:

- `logo-square.svg` (ForwardEmail logo for login and general branding)
- `favicon-16x16.png` / `favicon-32x32.png`
- `icon-192.png` / `icon-512.png` (gradient placeholders; replace with final maskable-safe icons when ready)

`manifest.json` references `icon-192.png` and `icon-512.png`; the page uses `logo-square.svg` and favicon PNGs.
